services:
  tls-proxy:
    image: nginx:1.27-alpine
    container_name: linkforge-nginx-tls
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./deploy/nginx/linkforge.local-https.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/certs:/etc/nginx/certs:ro

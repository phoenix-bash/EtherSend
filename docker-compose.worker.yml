services:
  conversion-worker:
    build:
      context: .
      dockerfile: Dockerfile.backend
    env_file:
      - ./deploy/backend.env
    command: ["pnpm", "--filter", "@linkforge/backend", "start"]
    restart: unless-stopped